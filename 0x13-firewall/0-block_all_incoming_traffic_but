#!/usr/bin/env bash
# configures a server to allow connection from 
# only port 22, 443, and 80

sudo apt-get update
sudo apt-get install ufw
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw default allow 344
sudo ufw default allow 22
sudo ufw default allow 80
sudo ufw enable
